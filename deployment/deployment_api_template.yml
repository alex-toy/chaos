apiVersion: apps/v1
kind: Deployment

metadata:
  name: deployment-$CI_PROJECT_NAME-$CI_COMMIT_BRANCH
  labels:
    app: lead-scoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lead-scoring
  
  template:
    metadata:
      labels:
        app: lead-scoring
    
    spec:
      containers:
      - name: lead_scoring_container
        image: eu.gcr.io/yotta-square-ml3/$CI_PROJECT_NAME:$CI_COMMIT_BRANCH-$CI_COMMIT_SHA
        ports:
        - containerPort: 5000
        volumeMounts:
        - name: secrets
          mountPath: "/secret"
          readOnly: true
      volumes:
      - name: secrets
        secret:
          secretName: chaos-secrets-4



